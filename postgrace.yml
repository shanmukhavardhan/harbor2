---
 - hosts: sql
   tasks:
   - name: install PostgreSQL version 11
     yum: pkg=postgresql11-server  state=present
     
   - name: start postgresql11-server services 
     service: name=postgresql11-server  state=started
     
